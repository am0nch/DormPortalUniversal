# Student data exports and backups (re-created by the app at runtime)
*.json
# But manifest.json is a PWA app file, not a data export
!manifest.json
*.xlsx
*.pdf

# Design previews (not part of the module system)
clearance_a5_preview.html

# VS Code workspace config (local only)
*.code-workspace

# Backend lives in its own private repo (DormPortalBackend)
backend/

# Serena MCP project files (local tooling only)
.serena/
serena/

# Obsidian vault config (local only)
.obsidian/

# Local planning docs — visible in Obsidian but not tracked in git
docs/

# Serena language server config (local tooling only)
language-servers.md
per-project-language-backend.md

# Local PDF generation script (hardcoded paths, reportlab dependency)
generate_security_doc.py

# Generated / unrelated scripts
DormPortal API - Swagger UI.html
DormPortal API - Swagger UI_files/
setup-deepseek.sh
